<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- In demo mode, activate the eSewa provider in Test Mode and
             pre-fill the sandbox test credentials so the payment flow can be
             exercised out of the box. -->
        <record id="payment_provider_esewa" model="payment.provider">
            <field name="state">test</field>
            <field name="is_published">True</field>
            <field name="esewa_merchant_id">EPAYTEST</field>
            <field name="esewa_secret_key">8gBm/:&amp;EnhH.1/q</field>
        </record>
    </data>
</odoo>
